{% block favorite_button_widget %}

    {% if product is defined %}
        {% set isFavorite = is_favorite_product(product) %}
        {% set options = {'product': product,'origin': origin} %}
    {% else %}
        {% set options = {} %}
    {% endif %}

    {% set heartSvgClass = isFavorite ? ' isFavorite' : '' %}
    {% set attr = layout_attr_defaults(attr, {
        'data-page-component-init-on': 'asap',
        'data-page-component-view': 'synoliafavorite/js/app/views/favorite-button-view-ajax',
        'data-page-component-options': options|json_encode,
        '~class': ' btn btn--plain btn--no-padding favorite-button favorite-button--' ~ class_prefix
    }) %}

    <button {{ block('block_attributes') }}>
        {% import '@OroUI/layouts/renderIcon.html.twig' as renderIcon %}
        {{ renderIcon.icon({name: 'heart', extraClass: 'favorite-button__icon' ~ heartSvgClass}) }}
    </button>
{% endblock %}
